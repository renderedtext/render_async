require "spec_helper"

describe RenderAsync do
  it "has a version number" do
    expect(RenderAsync::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
